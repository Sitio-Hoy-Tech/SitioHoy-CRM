"use client";
import CatalogoCRUD from "@/components/CatalogoCRUD";

export default function EstadosContactoPage() {
  return <CatalogoCRUD title="Estados de contacto" apiUrl="/api/catalogos/estados-contacto" />;
}
